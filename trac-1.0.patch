diff -Nau1r trac.orig/trac/ticket/notification.py trac.new/trac/ticket/notification.py
--- trac.orig/trac/ticket/notification.py	2015-10-11 18:55:09.503419239 +0200
+++ trac.new/trac/ticket/notification.py	2015-10-11 18:55:33.859323795 +0200
@@ -480,3 +480,7 @@
         if not self.newticket:
-            msgid = self.get_message_id(dest)
+            # Hack for Trac 1.0 to make trac-messageid-plugin work:
+            rows = self.env.db_query("""
+                    SELECT messageid FROM messageid WHERE ticket=%s
+                    """, (self.ticket.id,))
+            msgid = rows[0][0] if rows else self.get_message_id(dest)
             hdrs['In-Reply-To'] = msgid
